.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
# SVGs enviados em runtime (uploads do profissional) — o seed/ fica versionado.
storage/svgs/*.svg
# Imagens enviadas em runtime (thumbnail/banner do profissional).
storage/images/
